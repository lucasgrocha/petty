import React from 'react';

import './styles.css';
import Navbar from '../Navbar';

const Header: React.FC = () => {
  return (
    <div id="header">
      <Navbar />
    </div>
  );
};

export default Header;
